import React, { FunctionComponent } from 'react';

import { styled } from 'linaria/react';

const Wrapper = styled.div``;

type Props = {};

export const SendWidget: FunctionComponent<Props> = (props) => {
  return <Wrapper>SendWidget</Wrapper>;
};
