import type { FC } from 'react';

import { styled } from '@linaria/react';

const Wrapper = styled.div``;

interface Props {}

export const TransactionImageView: FC<Props> = (props) => {
  return <Wrapper>TransactionImageView</Wrapper>;
};
